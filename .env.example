# Local dev: point at your local or EC2 API
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# --- Vercel (recommended: HTTPS page → proxy → HTTP API on EC2) ---
# In Vercel Project → Settings → Environment Variables, set:
#   BACKEND_PROXY_URL=http://YOUR_EC2_PUBLIC_IP:8000
#   NEXT_PUBLIC_API_URL=/backend-api
# Production + http:// API: bleaching WebSocket is OFF unless you set NEXT_PUBLIC_WS_URL (wss://…).
# Local npm run dev can still use ws:// to an http:// API.
# For HTTPS pages + http API, set wss after TLS on the API, e.g. NEXT_PUBLIC_WS_URL=wss://api.example.com
